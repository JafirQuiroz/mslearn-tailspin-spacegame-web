node {
  stage('init') {
      echo 'Init...'
      checkout scm
  }
  stage('build') {
   echo 'Building...'
    steps {
      echo 'Running unit testing...'
    }
  }
  stage('deploy') {
    echo 'deploy..'
  }
}
